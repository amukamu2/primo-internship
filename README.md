# primo-internship

To set up dependency:

npm install typescript tsx jest ts-jest @types/jest @jest/globals

npx ts-jest config:init

To execute code, please use:

npx tsx merge.ts

To run unit test, please use:

npm test